# خطة فحص خدمة حماية حقوق المؤلف

## المرحلة 0 — التحضير
1. تشغيل الموقع محلياً والتأكد من الاتصال بقاعدة البيانات.
2. تجهيز حسابات الأدوار الخمسة (كلمة السر الموحدة `Moc@123456`): **FINANCE**، **STUDIES_ASSESSOR**، **STUDIES_HEAD**، **LEGAL_DIRECTOR**، **DEPUTY_MINISTER**، إضافة لحساب SUPER_ADMIN.
3. تجهيز إيميل حقيقي للاختبار (لاستقبال رسائل المواطن).
4. تشغيل الاختبارات الآلية الموجودة: `src/app/[locale]/services/copyright/page.test.mjs`.

## المرحلة 1 — نموذج المواطن (الواجهة العامة)
صفحة `/services/copyright`:

1. **التحقق من الحقول**: إرسال النموذج فارغاً → يجب أن تظهر رسائل خطأ لكل حقل إلزامي (والتأكد أن الفورم فيه `noValidate` حتى لا يحجب التحقق المخصص).
2. **نوع وثيقة الهوية**: اختيار "هوية وطنية" → يطلب وجه + ظهر؛ اختيار "جواز سفر" → صفحة واحدة فقط.
3. **صفة المتقدم**: تجربة كل صفة (مؤلف / وكيل / ورثة / شركة...) والتأكد أن الملفات الإضافية المطلوبة تتغير (سجل تجاري، وكالة، هوية الموكِّل...).
4. **رفع الملفات**: ملف صحيح يُقبل، ملف بنوع/حجم خاطئ يُرفض.
5. **الإرسال الناجح**: يظهر رمز المعاملة للمواطن + يصل **إيميل تأكيد الاستلام**.
6. **تتبع الطلب**: البحث برمز المعاملة يعرض الحالة؛ رمز خاطئ يعطي "لم يتم العثور".

## المرحلة 2 — دفع الرسم الأولي
1. من صفحة التتبع: تسديد الرسم الأولي (`pay_initial`) مع رفع إشعار الدفع → الحالة تصبح **finance_review**.
2. التأكد أن **إشعاراً وصل لحساب FINANCE فقط** في لوحة التحكم.
3. **فحص سلبي**: محاولة `pay_initial` مرة ثانية على نفس الطلب → يجب أن يرفض (409).

## المرحلة 3 — سير العمل الإداري (5 أدوار بالترتيب)
لكل خطوة: تنفيذ الإجراء + التحقق من (أ) تغيّر الحالة، (ب) وصول الإشعار للدور التالي **فقط**، (ج) وصول الإيميل للمواطن إن وُجد:

| # | الدور | الإجراء | الحالة الجديدة | ما يجب التحقق منه |
|---|-------|---------|----------------|--------------------|
| 1 | FINANCE | تأكيد الرسم الأولي | under_review | إيميل للمواطن **مع إيصال PDF الأولي** + إشعار للمقيّم |
| 2 | STUDIES_ASSESSOR | رفع التقرير الفني | تبقى under_review | إشعار لرئيس الدراسات |
| 3 | STUDIES_HEAD | رفع التوصيات | تبقى under_review | إشعار للمدير القانوني |
| 4 | LEGAL_DIRECTOR | الإحالة للموافقة النهائية | pending_final_approval | إيميل للمواطن + إشعار لمعاون الوزير |
| 5 | DEPUTY_MINISTER | الموافقة | pending_fees | **إيميل الموافقة مع رابط دفع الرسم النهائي** |
| 6 | المواطن | دفع الرسم النهائي (`pay_final`) | final_review | إشعار لـ FINANCE |
| 7 | FINANCE | تأكيد الدفع النهائي | completed | إيميل الإنجاز + **الإيصال النهائي PDF**، وpaymentStatus تصبح fully_paid |

- التأكد أن **مؤشر المراحل (stepper)** في لوحة التحكم يعكس كل خطوة، وأن مصغرات الملفات المرفوعة تفتح.

## المرحلة 4 — المسارات البديلة
1. **التعليق (suspended)**: تعليق طلب → يصل إيميل للمواطن → المواطن يعيد رفع المستندات (`resubmit`) → الحالة ترجع under_review + إشعار للمقيّم. فحص سلبي: `resubmit` على طلب غير معلّق → 409.
2. **الرفض (rejected)**: رفض طلب → يصل إيميل الرفض، ولا يستطيع المواطن الدفع بعدها.
3. **الحذف**: يعمل فقط بحساب SUPER_ADMIN، ويُسجَّل في سجل التدقيق (audit log).

## المرحلة 5 — الأمان (فحوص سلبية)
1. الوصول إلى `/api/admin/copyright-submissions` **بدون تسجيل دخول** → 401/403 (هذه كانت الثغرة السابقة — لازم تنفحص كل مرة).
2. `GET /api/copyright` **بدون رمز** → لا يعرض قائمة الطلبات أبداً (400).
3. إرسال `applicationStatus` غير موجود في القائمة المسموحة عبر PATCH → 400.
4. محاولة PATCH بحساب دور لا يملك MANAGE_SUBMISSIONS → 403.
5. **Rate limiting**: أكثر من 5 إرسالات للنموذج خلال 30 دقيقة من نفس الـ IP → 429.
6. محاولة المواطن تغيير الحالة مباشرة عبر PUT بأي `action` غير الثلاثة المسموحة → 400.

## المرحلة 6 — إيصال الـ PDF والإيميلات
1. فتح ملفي الإيصال (الأولي والنهائي) والتأكد من: الهوية البصرية للوزارة، صحة الأرقام والأسماء بالعربية، عدم وجود حروف مكسورة.
2. مراجعة كل الإيميلات (7 قوالب) على موبايل وحاسب: التأكيد، قيد الدراسة، الموافقة النهائية المعلقة، التعليق، الرفض، الموافقة/الرسوم، الإنجاز.
3. **ملاحظة للسيرفر**: توليد الـ PDF يحتاج Chromium مثبتاً (`dnf install chromium`) — يُفحص على السيرفر وليس محلياً فقط.

## المرحلة 7 — ما قبل النشر (على السيرفر)
1. التأكد من تنفيذ ترحيل قاعدة البيانات المعلّق: عمود `idDocType` وإعادة تسمية `idFile` إلى `idFileFront`.
2. تجربة معاملة كاملة واحدة (end-to-end) على بيئة الإنتاج بعد النشر.

---

**ترتيب التنفيذ المقترح**: المرحلة 5 (الأمان) والاختبار الآلي أولاً لأنهما الأسرع، ثم معاملة كاملة واحدة تغطي المراحل 1–3 و6 دفعة واحدة، ثم المسارات البديلة.
